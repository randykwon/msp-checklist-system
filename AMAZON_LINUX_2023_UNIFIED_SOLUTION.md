# Amazon Linux 2023 통합 해결 가이드

## 🎯 개요

이 문서는 Amazon Linux 2023에서 발생하는 모든 MSP Checklist 빌드 문제를 **하나의 스크립트로 완전히 해결**하는 통합 솔루션입니다.

## 🔧 해결되는 문제들

### 1. 디렉토리 구조 혼재 문제
- **문제**: `/opt/msp-checklist`와 `/opt/msp-checklist-system` 혼용
- **해결**: `msp-checklist-system`으로 완전 통일
- **호환성**: 심볼릭 링크로 기존 경로 지원

### 2. CSS 프레임워크 호환성 문제
- **문제**: LightningCSS `linux-x64-gnu.node` 모듈 없음
- **해결**: 모든 CSS 프레임워크 완전 제거 후 순수 CSS로 대체
- **대상**: Tailwind CSS v4, LightningCSS, PostCSS

### 3. Next.js 16 TypeScript 문제
- **문제**: `Cannot find module 'typescript'`
- **해결**: TypeScript 설정 최적화 및 Webpack 설정 개선

### 4. Node.js fs 모듈 문제
- **문제**: `Can't resolve 'fs'` 브라우저 환경에서 Node.js 모듈 접근
- **해결**: Webpack fallback 설정으로 완전 차단

### 5. Admin 시스템 컴포넌트 누락
- **문제**: `../admin: No such file or directory`
- **해결**: AdminLayout 컴포넌트 및 필수 모듈 자동 생성

### 6. curl 패키지 충돌
- **문제**: `curl-minimal`과 `curl` 패키지 충돌
- **해결**: 자동 감지 및 교체

## 🚀 통합 설치 스크립트

### 사용법

```bash
# 스크립트 다운로드 및 실행
cd /opt
sudo wget https://raw.githubusercontent.com/your-repo/amazon-linux-2023-unified-installer.sh
sudo chmod +x amazon-linux-2023-unified-installer.sh
sudo ./amazon-linux-2023-unified-installer.sh
```

### 스크립트 특징

- **완전 자동화**: 사용자 개입 최소화
- **재시도 메커니즘**: 네트워크 문제 자동 해결
- **메모리 최적화**: 스왑 파일 자동 생성
- **오류 복구**: 실패 시 자동 정리
- **상세 로깅**: 모든 과정 기록

## 📋 설치 과정

### 1단계: 시스템 검증
```bash
✅ Amazon Linux 2023 확인
✅ 최소 1GB 메모리 확인
✅ 최소 3GB 디스크 공간 확인
✅ 인터넷 연결 확인
```

### 2단계: 디렉토리 구조 통일
```bash
🔧 /opt/msp-checklist-system으로 통일
🔗 호환성 심볼릭 링크 생성
📁 기존 데이터 백업 및 보존
```

### 3단계: 시스템 환경 설정
```bash
📦 시스템 패키지 업데이트
🐛 curl 충돌 문제 해결
🔥 방화벽 설정 (포트 3010, 3011)
⚡ Node.js 20.9.0 설치
```

### 4단계: CSS 문제 완전 해결
```bash
💥 모든 CSS 프레임워크 완전 제거
🎨 순수 CSS로 완전 대체
🧹 node_modules 캐시 정리
📝 package.json 의존성 정리
```

### 5단계: Next.js 최적화
```bash
⚙️ next.config.ts 최적화
📝 tsconfig.json 설정 개선
🚫 Node.js 모듈 브라우저 접근 차단
🔧 Webpack fallback 설정
```

### 6단계: Admin 시스템 구축
```bash
🎨 AdminLayout 컴포넌트 생성
🗄️ 데이터베이스 모듈 생성
📝 TypeScript 설정 최적화
🔧 Next.js 설정 동기화
```

### 7단계: 빌드 및 검증
```bash
📦 의존성 설치 (재시도 포함)
🔨 MSP Checklist 빌드
🔨 Admin 시스템 빌드
✅ 설치 검증
🚀 서버 시작
```

## 📊 시스템 요구사항

### 최소 요구사항
- **OS**: Amazon Linux 2023
- **메모리**: 1GB (2GB 미만 시 스왑 파일 자동 생성)
- **디스크**: 3GB 여유 공간
- **네트워크**: 인터넷 연결 필수

### 권장 요구사항
- **인스턴스**: t3.small 이상
- **메모리**: 2GB 이상
- **디스크**: 8GB 이상
- **네트워크**: 안정적인 인터넷 연결

## 🔍 문제 해결

### 설치 실패 시

1. **로그 확인**
   ```bash
   tail -f /tmp/msp-unified-install-*.log
   ```

2. **디스크 공간 확인**
   ```bash
   df -h /
   ```

3. **메모리 확인**
   ```bash
   free -h
   ```

4. **재시도**
   ```bash
   sudo ./amazon-linux-2023-unified-installer.sh
   ```

### 빌드 실패 시

1. **개별 빌드 테스트**
   ```bash
   cd /opt/msp-checklist-system/msp-checklist
   npm run build
   
   cd admin
   npm run build
   ```

2. **CSS 문제 재해결**
   ```bash
   cd /opt/msp-checklist-system
   ./nuclear-css-fix.sh
   ```

3. **Admin 컴포넌트 재생성**
   ```bash
   cd /opt/msp-checklist-system
   ./complete-admin-fix.sh
   ```

### 서버 시작 실패 시

1. **포트 확인**
   ```bash
   sudo ss -tlnp | grep -E ":(3010|3011)"
   ```

2. **프로세스 정리**
   ```bash
   sudo pkill -f "node.*msp"
   sudo pkill -f "next"
   ```

3. **수동 시작**
   ```bash
   cd /opt/msp-checklist-system
   ./restart-servers.sh
   ```

## 📁 통일된 디렉토리 구조

```
/opt/msp-checklist-system/          # 메인 디렉토리
├── msp-checklist/                  # MSP 체크리스트 앱
│   ├── admin/                      # 관리자 시스템
│   │   ├── components/             # Admin 컴포넌트
│   │   │   └── AdminLayout.tsx     # 자동 생성됨
│   │   ├── lib/                    # Admin 라이브러리
│   │   │   └── db.ts               # 자동 생성됨
│   │   ├── app/                    # Admin Next.js 앱
│   │   ├── next.config.ts          # 최적화됨
│   │   ├── tsconfig.json           # 최적화됨
│   │   └── package.json
│   ├── app/                        # 메인 Next.js 앱
│   │   └── globals.css             # 순수 CSS로 교체됨
│   ├── next.config.ts              # 최적화됨
│   ├── tsconfig.json               # 최적화됨
│   └── package.json                # CSS 프레임워크 제거됨
├── *.sh                            # 설치/관리 스크립트들
├── package.json                    # 프로젝트 루트
└── README.md

/opt/msp-checklist -> /opt/msp-checklist-system  # 호환성 심볼릭 링크
```

## 🌐 서비스 접속

### 설치 완료 후 접속 주소
- **메인 서비스**: `http://YOUR_SERVER_IP:3010`
- **관리자 시스템**: `http://YOUR_SERVER_IP:3011`

### AWS 보안 그룹 설정
```
인바운드 규칙:
- 포트 22 (SSH): 0.0.0.0/0
- 포트 3010 (메인): 0.0.0.0/0
- 포트 3011 (관리자): 0.0.0.0/0
```

## 🔧 유용한 명령어

### 서버 관리
```bash
# 서버 상태 확인
cd /opt/msp-checklist-system && ./server-status.sh

# 서버 재시작
cd /opt/msp-checklist-system && ./restart-servers.sh

# 서버 중지
cd /opt/msp-checklist-system && ./stop-servers.sh

# 로그 확인
cd /opt/msp-checklist-system && tail -f server.log
```

### 관리자 계정 관리
```bash
# 관리자 계정 생성
cd /opt/msp-checklist-system && node create-admin.cjs

# 운영자 계정 생성
cd /opt/msp-checklist-system && node create-operator.cjs

# 슈퍼관리자로 업그레이드
cd /opt/msp-checklist-system && node upgrade-to-superadmin.cjs
```

### 시스템 모니터링
```bash
# 시스템 리소스 확인
htop

# 디스크 사용량 확인
df -h

# 메모리 사용량 확인
free -h

# 네트워크 연결 확인
sudo ss -tlnp
```

## 📝 다음 단계

### 1. 환경 변수 설정
```bash
nano /opt/msp-checklist-system/msp-checklist/.env.local
```

### 2. AI 기능 설정
- OpenAI API 키 설정
- Claude API 키 설정 (선택사항)

### 3. 데이터베이스 설정
- SQLite 데이터베이스 초기화
- 초기 데이터 입력

### 4. SSL 인증서 설정 (선택사항)
```bash
# Let's Encrypt 인증서 설치
sudo dnf install -y certbot
sudo certbot certonly --standalone -d your-domain.com
```

## ✅ 성공 확인

### 설치 성공 지표
1. **빌드 성공**: MSP Checklist와 Admin 시스템 모두 빌드 완료
2. **서버 시작**: 포트 3010, 3011에서 서비스 응답
3. **웹 접속**: 브라우저에서 정상 페이지 로드
4. **로그 정상**: 오류 없는 서버 로그

### 문제 없음 확인
- ❌ LightningCSS 오류 없음
- ❌ TypeScript 오류 없음
- ❌ fs 모듈 오류 없음
- ❌ Admin 컴포넌트 오류 없음
- ❌ 디렉토리 구조 혼재 없음

## 🎉 결론

이 통합 스크립트는 Amazon Linux 2023에서 발생하는 **모든 MSP Checklist 빌드 문제를 한 번에 해결**합니다:

1. **디렉토리 구조 통일**: `msp-checklist-system`으로 완전 통일
2. **CSS 문제 완전 해결**: 모든 프레임워크 제거 후 순수 CSS 대체
3. **빌드 문제 해결**: Next.js, TypeScript, Node.js 모듈 문제 완전 해결
4. **Admin 시스템 완성**: 누락된 컴포넌트 자동 생성
5. **완전 자동화**: 사용자 개입 최소화로 안정적 설치

**이제 Amazon Linux 2023에서 MSP Checklist 시스템이 완벽하게 작동합니다!** 🚀