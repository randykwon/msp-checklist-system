# MSP QnA 프로젝트 설정 가이드

## 프로젝트 개요

이 프로젝트는 AWS MSP 파트너 프로그램 준비를 위한 종합 체크리스트 웹 애플리케이션입니다.

## 디렉토리 구조

```
msp-qna/
├── .env                    # API 키 설정 파일
├── msp_data/              # MSP 관련 문서 및 자료
│   └── 7.x/               # 버전 7.x 관련 문서
│       ├── AWS 관리형 서비스 제공업체(MSP) 파트너 프로그램 검증 체크리스트 2024년 12월 버전 7.1.pdf
│       ├── AWS_MSP_First_Call_Deck_October_2025.pptx
│       ├── AWS Managed Service Provider (MSP) Program Self-Assessment_checklist.xlsx
│       ├── ITSM for MSPs.pptx
│       └── AWS MSP Program - Partner Journey.pdf
└── msp-checklist/         # Next.js 웹 애플리케이션
    ├── app/               # Next.js App Router 페이지
    ├── components/        # React 컴포넌트
    ├── data/             # 체크리스트 데이터
    ├── types/            # TypeScript 타입 정의
    └── README.md         # 애플리케이션 사용 가이드
```

## 환경 설정

### 1. Node.js 버전 업그레이드

현재 시스템의 Node.js 버전(18.12.1)은 Next.js 16의 최소 요구사항(20.9.0)을 만족하지 않습니다.

#### Option A: nvm 사용 (권장)

```bash
# nvm이 설치되어 있다면
nvm install 20.9.0
nvm use 20.9.0

# msp-checklist 디렉토리에서
npm install
npm run dev
```

#### Option B: 시스템 Node.js 업그레이드

공식 Node.js 웹사이트에서 LTS 버전(20.x) 다운로드:
https://nodejs.org/

### 2. 웹 애플리케이션 실행

```bash
cd msp-checklist

# 의존성 설치
npm install

# 개발 서버 시작
npm run dev
```

브라우저에서 http://localhost:3000 접속

### 3. 프로덕션 빌드

```bash
# 빌드
npm run build

# 프로덕션 서버 시작
npm start
```

## 주요 기능

### ✅ 체크리스트 관리
- 36개의 MSP 검증 항목 체계적 관리
- 7개 카테고리별 그룹화
- 항목별 상태 추적 (시작 안함/진행 중/완료/해당없음)

### 📊 진행률 추적
- 실시간 전체 진행률 대시보드
- 카테고리별 진행률 시각화
- 완료 항목 통계

### 🔍 스마트 필터링
- 상태별 필터 (전체/시작 안함/진행 중/완료/해당없음)
- 유형별 필터 (사전 요구사항/기술 검증)
- 키워드 검색

### 💾 자동 저장
- LocalStorage 기반 자동 저장
- 페이지 새로고침 시 데이터 유지
- 브라우저별 독립적인 데이터 관리

### 📝 메모 및 협업
- 항목별 메모 기능
- 담당자 지정
- 마지막 수정 시간 추적

## API 키 설정

`.env` 파일에는 다음 API 키들이 설정되어 있습니다:

```env
ANTHROPIC_API_KEY=sk-ant-api03-...
OPENAI_API_KEY=sk-proj-...
GEMINI_API_KEY=AIzaSy...
```

**보안 주의사항**:
- `.env` 파일은 절대 Git에 커밋하지 마세요
- `.gitignore`에 `.env`가 포함되어 있는지 확인하세요
- 프로덕션 환경에서는 환경 변수로 관리하세요

## 데이터 관리

### 데이터 백업

```javascript
// 브라우저 콘솔에서 실행
const data = localStorage.getItem('msp-checklist-data');
console.log(data);
// 출력된 JSON을 복사하여 파일로 저장
```

### 데이터 복원

```javascript
// 브라우저 콘솔에서 실행
const backupData = '...'; // 백업한 JSON 문자열
localStorage.setItem('msp-checklist-data', backupData);
location.reload();
```

### 데이터 초기화

```javascript
// 브라우저 콘솔에서 실행
localStorage.removeItem('msp-checklist-data');
location.reload();
```

## MSP 검증 체크리스트 카테고리

### 1. 사전 요구사항 제어 (4항목)
- APN 요구사항
- AWS 인증
- 고객 참조
- Well-Architected 검토

### 2. 고객 관리 (5항목)
- 온보딩 프로세스
- 초기 평가
- 운영 검토
- Well-Architected 검토
- 고객 만족도

### 3. 서비스 제공 (5항목)
- 서비스 카탈로그
- 변경 관리
- 릴리스 관리
- 인시던트 관리
- 문제 관리

### 4. 모니터링 및 옵저버빌리티 (4항목)
- 인프라 모니터링
- 애플리케이션 모니터링
- 로그 관리
- 알림

### 5. 보안 및 규정 준수 (5항목)
- IAM
- 데이터 보호
- 네트워크 보안
- 규정 준수
- 취약점 관리

### 6. 백업 및 재해 복구 (4항목)
- 백업
- 복구
- 재해 복구 계획
- 비즈니스 연속성

### 7. 비용 최적화 (4항목)
- 비용 가시성
- 예산 관리
- 리소스 최적화
- RI/SP

## 문제 해결

### Node.js 버전 오류
```
Error: You are using Node.js 18.12.1. For Next.js, Node.js version ">=20.9.0" is required.
```

**해결방법**: Node.js 20.9.0 이상으로 업그레이드

### 의존성 설치 오류
```bash
# npm 캐시 정리 후 재설치
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### 포트 충돌 (3000번 포트)
```bash
# 다른 포트에서 실행
PORT=3001 npm run dev
```

## 다음 단계

1. ✅ 웹 애플리케이션 실행 및 테스트
2. ✅ MSP 체크리스트 항목 검토
3. ⏳ 실제 준비 작업 시작
4. ⏳ 증빙 자료 수집 및 업로드
5. ⏳ ISSI 심사 준비

## 참고 자료

- [AWS MSP Partner Program](https://aws.amazon.com/partners/programs/msp/)
- [Next.js Documentation](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [TypeScript](https://www.typescriptlang.org/docs/)
